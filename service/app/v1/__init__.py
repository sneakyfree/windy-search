"""Canonical V1 agent-facing endpoints.

`/v1/search` (M1.8) is the entry point for the master plan §5 wire
contract. Subsequent V1 endpoints (`/v1/fetch` alias, `/v1/browse`,
`/v1/research`) land in M2+.
"""
