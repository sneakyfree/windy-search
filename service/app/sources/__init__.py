"""Pluggable search sources — own corpus + bridges.

The abstract base lives in `base.py`; concrete implementations live
alongside (`stubs.py` for M1 development + tests; `brave.py`, `google.py`,
etc. land in M2; `own_corpus.py` lands in M3).
"""
